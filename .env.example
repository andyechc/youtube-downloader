# Copia a .env y ajusta a tu máquina/dominio:
#   cp .env.example .env
#
# Puerto directo de la app (http://localhost:8000)
PORT=8000
# Puerto público del proxy inverso nginx
NGINX_PORT=80
# Dominio(s) al que responde nginx (separados por espacios)
NGINX_DOMAIN=localhost
